package ir.team.eventmanager.services.user;

import ir.team.eventmanager.dto.user.CreateUserDTO;

public interface CreateUserAndPersonByManager {

    boolean createUserAndPersonByManager(CreateUserDTO userDTO);
}
